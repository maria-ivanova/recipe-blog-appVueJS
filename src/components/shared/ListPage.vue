<template>
  <div>
    <!-- BANNER -->
    <section class="sec banner_sec">
      <div class="banner_item inner_banner_item">
        <h2 class="title">{{ categoryName }}</h2>
      </div>
    </section>

    <!-- ITEMS LIST -->
    <section class="sec content_sec">
      <div class="container">
        <ItemsList :secondFilterCriterion="categoryName" filerCriterion="category"/>
      </div>
    </section>
  </div>
</template>

<script>
import ItemsList from '../shared/ItemsList.vue'

export default {
  name: "ListPage",
  components: {
    ItemsList
  },
  data() {
    return {};
  },
  computed: {
    categoryName() {
      return this.$route.params.category || 'Всички рецепти';
    },
  },
};
</script>

<style scoped>
.banner_item {
  position: relative;
  display: -webkit-box;
  display: -moz-box;
  display: -ms-flexbox;
  display: -webkit-flex;
  display: flex;
  -webkit-flex-direction: column;
  -ms-flex-direction: column;
  flex-direction: column;
  -webkit-flex-wrap: wrap;
  flex-wrap: wrap;
  -webkit-align-items: center;
  align-items: center;
  -webkit-justify-content: center;
  justify-content: center;
  width: 100%;
  min-height: 300px;
  padding: 50px 0;
  background: url(../../assets/images/banner2.jpg) no-repeat 50% 50%;
  background-attachment: fixed;
  background-size: cover;
  color: #fff;
  font-weight: 700;
  letter-spacing: 1px;
  text-shadow: 1px 9px 16px rgba(0, 0, 0, 0.18);
}

.banner_item .title {
  position: relative;
  display: inline-block;
  margin: 0;
  font-size: 62px;
  line-height: 92px;
  z-index: 2;
}

.banner_item::after {
  position: absolute;
  display: inline-block;
  content: "";
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: 1;
}

@media (max-width: 700px) {
  .banner_item .title {
    font-size: 52px;
    line-height: 72px;
  }
}
</style>